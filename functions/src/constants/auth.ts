export const X_USER_TOKEN_ID = "x-user-token-id";

export const RESPONSE_MESSAGES = {
  NOT_AUTHORIZED: "You aren't authorized",
  HAVE_NO_PERMISSIONS: "You have no permission no call this endpoint",
} as const;
